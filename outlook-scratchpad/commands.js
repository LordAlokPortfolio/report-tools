// Function file required by the Outlook ribbon command surface declared in
// manifest.xml. The scratchpad only uses a ShowTaskpane action today, so
// there are no ExecuteFunction commands to register yet — this file exists
// to satisfy the manifest's FunctionFile requirement.
Office.onReady(() => {
  // No ExecuteFunction commands registered.
});
