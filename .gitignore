# Real purchaser email - never pushed to this public repo.
# Copy data/company-email.example.json to data/company-email.json
# and fill in the real address; it stays local only.
data/company-email.json
